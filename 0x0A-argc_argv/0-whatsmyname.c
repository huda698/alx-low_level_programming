#include <stdio.h>

/**
 * main - print the name of program.
 * @argc: array size.
 * @argv: array to string.
 * Return: 0.
 */

int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
