#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers.
 *
 * @a: first input.
 *
 * @b: second input.
 *
 * Return: the value of mult.
 */

int mul(int a, int b)
{
	unsigned long mult;

	mult = a * b;
	return (mult);
}
